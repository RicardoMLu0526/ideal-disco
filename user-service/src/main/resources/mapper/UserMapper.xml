<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pdd.user.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.pdd.user.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM tb_user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM tb_user WHERE username = #{username}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT * FROM tb_user WHERE phone = #{phone}
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT * FROM tb_user WHERE email = #{email}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM tb_user
    </select>

    <insert id="insert" parameterType="com.pdd.user.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_user (username, password, phone, email, nickname, avatar, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{phone}, #{email}, #{nickname}, #{avatar}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.pdd.user.entity.User">
        UPDATE tb_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tb_user WHERE id = #{id}
    </delete>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM tb_user WHERE username = #{username}
    </select>

    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(*) > 0 FROM tb_user WHERE phone = #{phone}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM tb_user WHERE email = #{email}
    </select>

</mapper>
